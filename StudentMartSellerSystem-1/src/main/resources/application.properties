# ===============================
# = DATA SOURCE
# ===============================

# Set here configurations for the database connection

# Connection url for the database "Inventory_project"
#spring.datasource.url = jdbc:mysql://lab2.cf8hjynepyqw.us-east-2.rds.amazonaws.com:3306/inventory_project

# Username and password
#spring.datasource.username = root
#spring.datasource.password = 3961516jim

spring.datasource.url=jdbc:mysql://yunweiteam2.cwd2jbhq4mwd.us-east-2.rds.amazonaws.com:3306/ICSI518Team2?useSSL=false
spring.datasource.username=root
spring.datasource.password=team2_2018

# ===============================
# = JPA / HIBERNATE
# ===============================

# Use spring.jpa.properties.* for Hibernate native properties (the prefix is
# stripped before adding them to the entity manager).

# Show or not log for each sql query
spring.jpa.show-sql = true

# Hibernate ddl auto (create, create-drop, update): with "update" the database
# schema will be automatically updated accordingly to java entities found in
# the project
spring.jpa.hibernate.ddl-auto = update

# Naming strategy
spring.jpa.hibernate.naming-strategy = org.hibernate.cfg.ImprovedNamingStrategy

spring.jpa.open-in-view=false

# Allows Hibernate to generate SQL optimized for a particular DBMS
#spring.jpa.properties.hibernate.dialect = org.hibernate.dialect.MySQL5Dialect


#Set the port to 9090
server.port = 9090

#Define the max upload file size
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB